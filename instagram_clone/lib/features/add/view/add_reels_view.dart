import 'package:flutter/material.dart';

class AddReelsView extends StatelessWidget {
  const AddReelsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Add Reels View"),
      ),
    );
  }
}
